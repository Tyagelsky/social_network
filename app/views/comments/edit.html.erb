<h1>Edit comment</h1>
<%= form_for @comment, :url => update_comment_path({article_id: @article.id})  do |f| %>
    <div class="field">

    <%= f.text_area :content %>
  </div>
  <%= f.submit "OK", class: "btn btn-large btn-primary" %>
<% end %>
