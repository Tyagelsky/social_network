<%= form_for @article, url: {action: "show"} do |f| %>
  <div class="field">
    <%= f.text_area :body, placeholder: "New article.." %>
  </div>
  <%= f.submit "OK", class: "btn btn-large btn-primary" %>


<% end %>
