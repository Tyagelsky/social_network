<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <h3><%=  image_tag user.avatar.url(:thumb) %>
          <%= link_to user.username, user_path(user) %></h3>
    </li>
  <% end %>
</ul>
